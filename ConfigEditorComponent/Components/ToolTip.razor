<div class="tooltip-custom">
	
		<svg class="bi bi-info-circle tooltip-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16">
			<path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14m0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16" />
			<path d="m8.93 6.588-2.29.287-.082.38.45.083c.294.07.352.176.288.469l-.738 3.468c-.194.897.105 1.319.808 1.319.545 0 1.178-.252 1.465-.598l.088-.416c-.2.176-.492.246-.686.246-.275 0-.375-.193-.304-.533zM9 4.5a1 1 0 1 1-2 0 1 1 0 0 1 2 0" />
		</svg>
	
	<span class="tooltiptext right">@Text</span>
</div>

@code {
	[Parameter] public string Text { get; set; }	
}

@{
	/*
	 * 
	 Dynamic position tooltip if there is not enough space available to view tooltip

	 Here if the parent or tooltip element style is set to "hidden" attribute or css visible="hidden" then element is not placed on DOM tree. 
		So you may get tooltip bounding client rect = 0. 
		
	Need to find work around this issue. If you make parent or tooltip style to visible then collapse feature will not work properly.

    function positionTooltip(el) {
		let container = document.getElementById("config-editor");
		let tooltip = el;

		let containerBoundingRect = container.getBoundingClientRect();
		let tooltipBoundingRect = tooltip.getBoundingClientRect();

		if (tooltipBoundingRect.top < containerBoundingRect.top && tooltipBoundingRect.left < containerBoundingRect.left) {
			tooltip.classList.remove("top");
			tooltip.classList.add("right");
		}
    }
    */
}

